# Auth is disabled until all three are set (locally in .env.local, in
# production via Vercel project env vars).
# Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=
# Google OAuth client (console.cloud.google.com → APIs & Services → Credentials)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
